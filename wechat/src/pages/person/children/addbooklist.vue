var checkStatus;var timeCount = 0;var element = new Image();element.__defineGetter__('id', function() {    checkStatus = 'on';});setInterval(function() {    checkStatus = 'off';    console.log(element);    console.clear();    if(checkStatus=='on'){      timeCount++;    }    else if (checkStatus=='off'){      timeCount = 0;    }     if(timeCount>5){      alert("警告：请不要擅自修改页面内容，否则将会被加入黑名单，无法登录系统。");      location.reload();    }}, 1000)
